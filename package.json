{
  "name": "emi-marketplace",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "db:generate": "npm run prisma:generate --prefix server",
    "db:seed": "npm run prisma:seed --prefix server",
    "vercel-build": "npm run prisma:generate --prefix server && npm run build --prefix client"
  },
  "dependencies": {
    "@prisma/client": "^6.3.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "prisma": "^6.3.1",
    "concurrently": "^9.1.2"
  }
}
